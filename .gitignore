# ============================================================================
#  SECRETS — never commit real credentials. .env.example (placeholders) is kept.
# ============================================================================
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*credential*
*secret*
*token*
service_account*.json
gha-creds-*.json

# ============================================================================
#  DATA / OUTPUTS — regenerable, large. NOTE the leading slash: it anchors to the
#  repo ROOT so it does NOT match the tagent/data/ SOURCE package (which is code).
# ============================================================================
/data/
/data/_moneyup_advisor/
/outputs/
/factsheets/
/reports/
/dist/

# ============================================================================
#  MEDIA — downloaded clips + sampled chart frames (regenerated by the pipeline)
# ============================================================================
*.mp4
*.m4a
*.wav
*.mp3
*.png
*.jpg
*.jpeg

# ============================================================================
#  MODELS — large binaries (Whisper / VLM / weights)
# ============================================================================
/models/
*.pt
*.gguf
*.bin

# ============================================================================
#  Python / tooling
# ============================================================================
__pycache__/
*.pyc
*.pyo
.pytest_cache/
venv/
.venv/
*.log
.DS_Store

# ============================================================================
#  Node deps (only for scripts/verify_chart_browser.mjs)
# ============================================================================
node_modules/
package-lock.json
